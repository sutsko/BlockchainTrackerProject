@model IEnumerable<Asp.netCoreMVCCrud1.Models.Sector>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SectorName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SectorName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SectorId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SectorId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SectorId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
